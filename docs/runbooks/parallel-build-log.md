# Parallel-build log

Append one line per fan-out. Format:

```
YYYY-MM-DD — task summary — N agents parallel — Xm wallclock vs ~Ym sequential — Z conflicts.
```

This dataset tells us, over time, when fan-out actually pays off vs when the overhead drowns the gain.

---
